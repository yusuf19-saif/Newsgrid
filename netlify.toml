[build]
  command = "npm run build"
  publish = ".next"

[functions]
  # Extend function timeout to maximum allowed on free tier (26 seconds)
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-nextjs"

